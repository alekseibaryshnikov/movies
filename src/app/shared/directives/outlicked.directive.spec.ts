import { OutlickedDirective } from './outlicked.directive';

describe('OutlickedDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlickedDirective();
    expect(directive).toBeTruthy();
  });
});
